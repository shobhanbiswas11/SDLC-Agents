# Ask User Tool
name: ask_user
description: Ask the user a question and wait for their response. Use this for confirmation before applying refactors.
 
parameters:
  type: object
  properties:
    question:
      type: string
      description: The question to ask the user
  required:
    - question