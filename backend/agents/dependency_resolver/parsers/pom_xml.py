"""Parser for Maven pom.xml (stub — M8)."""
from typing import List, Tuple


class PomXmlParser:
    def parse(self, content: str) -> List[Tuple[str, str]]:
        raise NotImplementedError("Maven parser coming in M8")
