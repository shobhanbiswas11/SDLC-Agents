"""Parser for Rust Cargo.toml (stub — M8)."""
from typing import List, Tuple


class CargoTomlParser:
    def parse(self, content: str) -> List[Tuple[str, str]]:
        raise NotImplementedError("Cargo parser coming in M8")
