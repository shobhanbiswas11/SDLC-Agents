# Sample Flask App

A minimal Flask web server used to test the Containerization AI Agent.

## Run locally

```bash
pip install -r requirements.txt
python app.py
```

App listens on **port 5000**.

## Endpoints

| Route | Description |
|---|---|
| `GET /` | Returns a hello message |
| `GET /health` | Health check endpoint |
